# GSSTools
Tools around the ESA/Serco GSS

The `gist` directory contains scripts that are not fully ready for general use. They may lack help output, control by options alltogether, they may have some values hard-coded for use in my environment.

